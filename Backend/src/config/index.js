'use strict';

// Local imports. ==================================================================================
const { configRoutes } = require('./config.routes');
const { languageOptions } = require('./config.api');


module.exports = {
  configRoutes,
  languageOptions,
};
